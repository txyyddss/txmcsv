blocking-commands:
  - # The message key to send to users. You need to set the message in locale configs.
    blocked-message: no-suicide
    # The commands to block. Using regex.
    commands:
      - ^(.+:)?suicide$
      - ^(.+:)?kill$
    console-user-excluded: true
module-enabled: false
